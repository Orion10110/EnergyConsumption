@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    
    <script type="text/javascript">
    function OnSuccess(data) {
        var results = $('#results'); // получаем нужный элемент
        results.empty(); //очищаем элемент
        for (var i = 0; i < data.length; i++) {
            results.append('<li>' + data[i].Name + '</li>'); // добавляем данные в список
        }
    }
    </script>
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Здравствуйте, " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
        <li class="dropdown">
            @*<a class="btn dropdown-toggle" data-toggle="dropdown" href="#"> string.Format("<span>Purchase</span><em>{0:c}</em>", Model.thisItem.ItemSalePrice)
    Квартиры*@
            @*<li class="dropdown">
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">Page 1 <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li><a href="#">Page 1-1</a></li>
                    <li><a href="#">Page 1-2</a></li>
                    <li><a href="#">Page 1-3</a></li>
                </ul>
            </li>*@

            @Ajax.ActionLink(string.Format("apartments"), "HomeList", "MyHome", null, new AjaxOptions { UpdateTargetId = "myhome", LoadingElementId = "loading",
           LoadingElementDuration = 1000 }, htmlAttributes: new { @class = "dropdown-toggle", data_toggle = "dropdown"})
            <ul class="dropdown-menu" id="myhome">
                <li id="loading" style="display:none; text-align: center;">
                    <img src="~/Content/Images/ajax-loader.gif" />
                </li>
            </ul>
            
        </li>
        <li><a href="javascript:document.getElementById('logoutForm').submit()">Выйти</a></li>
    </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink("Регистрация", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
        <li>@Html.ActionLink("Выполнить вход", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
}

